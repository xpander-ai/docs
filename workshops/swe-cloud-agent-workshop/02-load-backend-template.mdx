---
title: "Module 2: Frontend Build & Testing"
description: "Use a lightweight ChatGPT-Codex style UI to interact with your coding agent"
icon: "desktop"
---

<Note>
  ### **Module Summary**

  - **Goal**: Clone a minimal ChatGPT-Codex style frontend and connect it to **xpander.ai as your backend service**
  - **Estimated Time**: 15–20 minutes
  - **Prerequisites**: React knowledge, completed backend setup from Module 1
</Note>

---

## 🧠 Architecture Reminder

In this module, you're using xpander.ai **purely as a Backend-as-a-Service (BaaS) platform** .

The frontend you'll run is not part of the xpander.ai platform — it’s a standalone React app that mimics the ChatGPT-Codex UI and uses the **xpander.ai SDK** to communicate with your configured agent.

---

## 🧱 Step 1: Clone the Frontend Example App

We’ll start by cloning and running the prebuilt UI:

```bash
git clone https://github.com/xpander-ai/coding-agent-frontend-example-app.git
cd coding-agent-frontend-example-app
npm install
```

![Step 1 - Clone and Install](/images/workshops/git-clone.png)

---

## 🔐 Step 2: Configure Environment

Copy the example environment file and populate it with your credentials from [xpander.ai Cloud](https://app.xpander.ai):

```bash
mv .env.example .env
```

Fill in the following:

```env
VITE_APP_API_KEY=your_api_key_here # Available in xpander.ai UI from Settings -> API keys 
VITE_APP_AGENT_ID=your_agent_id_here # Available in xpander.ai UI from the Workbench -> SDK node at the top of the canvas
VITE_APP_ORGANIZATION_ID=your_organization_id_here # Available in xpander.ai UI from Settings -> API keys
```

### Get Your Organization ID & API Key
<Frame>
  <video autoPlay muted loop playsInline className="w-full aspect-video" src="https://assets.xpanderai.io/workshops/videos/settings-api-keys.mp4" />
</Frame>

### Copy Your Agent ID
<Frame>
  <video autoPlay muted loop playsInline className="w-full aspect-video" src="https://assets.xpanderai.io/workshops/videos/swe-copy-agent-id.mp4" />
</Frame>
---

## ⚙️ Step 3: Start the App

Launch the frontend locally:

```bash
npm run dev
```

![Step 3 - npm run dev](/images/workshops/swe-npm-run-dev.png)

Then navigate to:

```
http://localhost:3000
```

![Step 3 - Open Browser](/images/workshops/swe-blank-tasks-manager.png)

---

## ✍️ Step 4: Send Your First Coding Task

From the input field, send one of the example prompts below:

<Accordion title="🧠 Prompt Examples for apps-by-agents">

### ✅ Todo List App
```text
Add a simple, cool and beautiful todo list app (HTML, CSS, JS) to xpander-ai/apps-by-agents.
````

### ⏱️ Pomodoro Timer

```text
Add a minimal Pomodoro timer app (HTML, CSS, JS) to xpander-ai/apps-by-agents.
```

### 🌦️ Weather Dashboard

```text
Build a weather dashboard using OpenWeatherMap API (HTML, CSS, JS) and add it to xpander-ai/apps-by-agents.
```

### 📝 Markdown Editor

```text
Create a simple markdown editor with live preview (HTML, CSS, JS) and save it in xpander-ai/apps-by-agents.
```

### 🧮 Calculator App

```text
Generate a calculator app with keyboard support (HTML, CSS, JS) and store in xpander-ai/apps-by-agents.
```

### 🌐 Landing Page

```text
Add a responsive landing page template with Tailwind CSS to xpander-ai/apps-by-agents.
```

### 💬 Chatbot UI

```text
Create a basic chatbot UI (HTML, CSS, JS) with a placeholder for messages and append it to xpander-ai/apps-by-agents.
```

### 🎨 Color Palette Generator

```text
Build a color palette generator that shows hex codes (HTML, CSS, JS) and commit to xpander-ai/apps-by-agents.
```

### 📏 Unit Converter

```text
Develop a unit converter (length, weight, temperature) as a single-page app and push to xpander-ai/apps-by-agents.
```

### ⌨️ Typing Speed Test

```text
Add a typing speed test app (HTML, CSS, JS) to xpander-ai/apps-by-agents.
```

### 🃏 Memory Card Matching Game

```text
Create a memory card matching game (HTML, CSS, JS) and drop it into xpander-ai/apps-by-agents.
```

</Accordion>

<Frame>
  <video autoPlay muted loop playsInline className="w-full aspect-video" src="https://assets.xpanderai.io/workshops/videos/swe-send-task.mp4" />
</Frame>

---

## 🔍 Step 5: Track Task Execution

Click **View** to view real-time execution details.

<Frame>
  <video autoPlay muted loop playsInline className="w-full aspect-video" src="https://assets.xpanderai.io/workshops/videos/swe-track-result.mp4" />
</Frame>

### You’ll see:

- **Top**: The original prompt or task
- **Popup**: Logs and generated output from the agent
- **Result**: Execution result
- **Logs & Messages Buttons**: Execution messages logs & codex execution logs


<Tip>
  The agent runs **OpenAI Codex** in a **secure, isolated sandbox** powered by **xpander.ai**.
  GitHub access is automatically inherited through the App connector you enabled during the import process.
</Tip>


## ✅ What's Next?

Your frontend is now live and connected to the xpander.ai BaaS. You prompted the agent to generated its first coding task :)

→ Proceed to [Module 3: Competition Submission](#)\

→ Optional: Try customizing the UI or agent behavior in Module 4