---
title: "Module 2: Frontend Build & Testing"
description: "Use a lightweight Codex-style UI to interact with xpander.ai as a Backend-as-a-Service"
icon: "desktop"
---

<Note>

### **Module Summary**

- **Goal**: Clone a minimal Codex-style frontend and connect it to **xpander.ai as your backend service**
- **Estimated Time**: 15–20 minutes  
- **Prerequisites**: React knowledge, completed backend setup from Module 1

</Note>

---

## 🧠 Architecture Reminder

In this module, you're using **xpander.ai purely as a Backend-as-a-Service (BaaS)**.

The frontend you'll run is **not part of xpander** — it’s a standalone React app that mimics the OpenAI Codex playground UI and uses the **xpander.ai SDK** to communicate with your configured agent.

---

## 🧱 Step 1: Clone the Frontend Example App

We’ll start by cloning and running the prebuilt UI:

```bash
git clone https://github.com/xpander-ai/coding-agent-frontend-example-app.git
cd coding-agent-frontend-example-app
npm install
````

![Step 1 - Clone and Install](/images/workshops/git-clone.png)

---

## 🔐 Step 2: Configure Environment

Copy the example environment file and populate it with your credentials from [xpander.ai Cloud](https://app.xpander.ai):

```bash
mv .env.example .env
```

Fill in the following:

```env
VITE_APP_API_KEY=your_api_key_here
VITE_APP_AGENT_ID=your_agent_id_here
VITE_APP_ORGANIZATION_ID=your_organization_id_here
```

These values are available under your account settings in the xpander.ai platform.

<Frame>
  <video
    autoPlay
    muted
    loop
    playsInline
    className="w-full aspect-video"
    src="https://assets.xpanderai.io/workshops/videos/settings-api-keys.mp4"
  ></video>
</Frame>

---

## ⚙️ Step 3: Start the App

Launch the frontend locally:

```bash
npm run dev
```
![Step 3 - npm run dev](/images/workshops/swe-npm-run-dev.png)

Then navigate to:

```
http://localhost:3000
```

![Step 3 - Open Browser](/images/workshops/swe-blank-tasks-manager.png)

---

## ✍️ Step 4: Send Your First Coding Task

From the input field, send a prompt like:

```text
Add a simple, cool and beautiful todo list app (HTML, CSS, JS) to xpander-ai/apps-by-agents. do it in one-shot
```

<Frame>
  <video
    autoPlay
    muted
    loop
    playsInline
    className="w-full aspect-video"
    src="https://assets.xpanderai.io/workshops/videos/swe-send-task.mp4"
  ></video>
</Frame>

---

## 🔍 Step 5: Track Task Execution

Click **View** to view real-time execution details.

<Frame>
  <video
    autoPlay
    muted
    loop
    playsInline
    className="w-full aspect-video"
    src="https://assets.xpanderai.io/workshops/videos/swe-track-result.mp4"
  ></video>
</Frame>

### You’ll see:

* **Top**: The original prompt or task
* **Popup**: Logs and generated output from the agent
* **Result**: Execution result
* **Logs & Messages Buttons**: Execution messages logs & codex execution logs

---

<Tip>

The agent runs **OpenAI Codex** in a **secure, isolated sandbox** powered by **xpander.ai**.
GitHub access is automatically inherited through the agentic interface you connected during the import process — no tokens needed.

</Tip>

---

## ✅ What's Next?

Your frontend is now live and connected to the xpander.ai BaaS. and generated its first coding task :)

→ Proceed to [Module 3: Competition Submission](#)<br/>
→ Optional: Try customizing the UI or agent behavior in Module 4