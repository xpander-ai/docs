---
title: "LLMMetrics"
description: "Detailed LLM usage metrics"
icon: "chart-simple"
---

`LLMMetrics` extends `LLMTokens` with additional context about an LLM call.

## Properties

| Property | Type | Description |
|----------|------|-------------|
| `completion_tokens` | `number` | Tokens produced by the model |
| `prompt_tokens` | `number` | Tokens from the prompt |
| `total_tokens` | `number` | Total tokens used |
| `provider` | `LLMProvider` | LLM provider name |
| `model` | `string` | Model identifier |
| `function_name` | `string[]` | Functions executed, if any |
| `duration` | `number` | Inference time in milliseconds |
| `finish_reason` | `string` | LLM finish reason |
| `source_node_type` | `string` | Source node type in the graph |

## Example

```python Python
from xpander_sdk import LLMMetrics, LLMProvider

metrics = LLMMetrics(provider=LLMProvider.OPEN_AI, model="gpt-4.1")
print(metrics.total_tokens)
```

<Note>
For all metric fields, consult the
<Link href="https://github.com/xpander-ai/xpander-sdk/blob/main/docs/python/LLMMetrics.md">xpander-sdk docs</Link>.
</Note>
